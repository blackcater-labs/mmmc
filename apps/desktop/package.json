{
  "private": true,
  "name": "@mmmc/desktop",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:ui": "pnpm run --filter web dev",
    "build": "tauri build",
    "build:ui": "pnpm run --filter web build && cpx ../web/dist dist",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "^1.3.1",
    "cpx": "^1.5.0"
  }
}
